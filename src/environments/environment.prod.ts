export const environment = {
  production: true,
  /** Set this to your deployed Render / Railway / Fly.io URL before building for production. */
  defaultApiUrl: 'https://your-email-service.onrender.com'
};
